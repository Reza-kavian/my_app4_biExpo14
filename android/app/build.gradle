apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: "com.facebook.react"

react {
    autolinkLibrariesWithApp()
}

def enableProguardInReleaseBuilds = false
def jscFlavor = 'io.github.react-native-community:jsc-android:2026004.+'
def isNewArchitectureEnabled = rootProject.hasProperty("newArchEnabled") && rootProject.getProperty("newArchEnabled").toBoolean()

android {
    compileSdk rootProject.ext.compileSdkVersion
    namespace "com.my_app4_biexpo14"

    defaultConfig {
        applicationId "com.my_app4_biexpo14"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"

        // ** 👈 گام ۱: این بلوک را اضافه کنید تا معماری‌های مورد نیاز مشخص شود **
        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a", "x86", "x86_64"
        }

        // 🎯 تغییر اصلی: پیکربندی CMake باید درون defaultConfig انجام شود
        externalNativeBuild {
            cmake {
                // این متد (arguments) اکنون در اینجا قابل دسترسی است
                arguments "-DANDROID_TOOLCHAIN=clang",
                          "-DANDROID_STL=c++_shared",
                          "-DHERMES_ENABLE_DEBUGGER=TRUE",
                          "-DHERMES_ENABLE_BYTECODE_STRIPPING=FALSE",
                          // --- FIX CRITICAL: Use the reliable react.reactNativeDir ---
                          "-DREACT_NATIVE_DIR=${react.reactNativeDir}"
                          // ----------------------------------------
            }
        }
    }

    signingConfigs {
        debug {
            storeFile file('debug.keystore')
            storePassword 'androiddebugkey'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled enableProguardInReleaseBuilds
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation("com.facebook.react:react-android")
    if (hermesEnabled.toBoolean()) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation jscFlavor
    }
    implementation project(':react-native-reanimated')
    implementation project(':react-native-worklets-core')
    implementation project(':react-native-worklets')
    // ✅ موتور اصلی بارکدخوان برای Vision Camera V4 (حیاتی)
    implementation 'com.google.mlkit:barcode-scanning:17.2.0'
}